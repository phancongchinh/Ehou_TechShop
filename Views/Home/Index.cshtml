@model IEnumerable<TechShop.Models.Entity.Product>

@{
    ViewData["Title"] = "Home Page";
}

<div class="d-flex">
    @foreach (var i in Model)
    {
        @await Html.PartialAsync("_CardProduct", i)
    }
</div>